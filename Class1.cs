using System;

public class ComponentLoader
{
	public SetComponent(this ITemplate tpl, ITemplate componentTemplate)
	{
		// Identify component name in tpl
		// Load params from tpl
		// Set params to component
		// Set component to tpl
	}
}
